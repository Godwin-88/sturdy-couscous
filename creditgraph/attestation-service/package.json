{
  "name": "creditgraph-attestation-service",
  "version": "0.1.0",
  "private": true,
  "description": "Attestcoin protocol evidence verifier for CreditGraph — wraps the real @gluwa/usc-sdk (transaction inclusion proofs: Merkle + continuity).",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@gluwa/usc-sdk": "^0.18.0",
    "ethers": "^6.15.0",
    "fastify": "^5.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}